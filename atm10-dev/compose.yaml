services:
  mc:
    container_name: atm10-dev
    image: itzg/minecraft-server:java25-graalvm
    restart: unless-stopped
    stop_grace_period: 60s
    user: ${UID}:${GID}
    tty: true
    stdin_open: true
    depends_on:
      - nginx
      - db
    networks:
      default:
      router:
        aliases:
          - atm10-dev
    ports:
      - name: simplevoicechat
        target: 24455
        published: 24455
        protocol: udp
    volumes:
      - type: bind
        source: /opt/minecraft/servers/atm10-dev
        target: /data
      - type: bind
        source: ../../resources/atm10/config
        target: /config
        read_only: true
      - type: bind
        source: ./extras
        target: /extras
        read_only: true
    env_file:
      - path: ./.env
        required: true
    environment:
      MODPACK_PLATFORM: AUTO_CURSEFORGE
      CF_SLUG: all-the-mods-10
      CF_FILENAME_MATCHER: 5.3.1
      MODE: creative
      MEMORY: 8G
      MOTD: |
        Running %MODPACK_NAME% version %env:MODPACK_VERSION%
      RCON_CMDS_STARTUP: |
        gamerule doFireTick false
        gamerule playerSleepingPercentage 0
      RCON_CMDS_FIRST_CONNECT: |-
        chunky pause
      RCON_CMDS_LAST_DISCONNECT: |-
        chunky continue
      SYNC_SKIP_NEWER_IN_DESTINATION: "false"
      MODRINTH_PROJECTS: "@/extras/modrinth-mods.txt"
      MODRINTH_DOWNLOAD_DEPENDENCIES: required
    labels:
      mc-router.host: atm10-dev.off-topic.chat
      mc-router.network: minecraft-shared_default

  nginx:
    image: webdevops/php-nginx:8.4-alpine
    restart: unless-stopped
    networks:
      default:
      proxy:
        aliases:
          - atm10-dev
    volumes:
      - type: bind
        source: ./vhost.conf
        target: /opt/docker/etc/nginx/vhost.conf
        read_only: true
      - type: bind
        source: /opt/minecraft/servers/atm10-dev/bluemap/web
        target: /app
        read_only: true
    environment:
      DATABASE: ${CFG_DB_NAME}
      USERNAME: ${CFG_DB_USER}
      PASSWORD: ${CFG_DB_PASSWORD}
    labels:
      traefik.enable: "true"
      traefik.http.routers.atm10-dev.rule: Host(`atm10-dev.off-topic.chat`)
      traefik.http.routers.atm10-dev.service: atm10-dev
      traefik.http.services.atm10-dev.loadbalancer.server.url: http://atm10-dev:80

  db:
    image: mariadb:11
    restart: unless-stopped
    volumes:
      - type: volume
        source: db
        target: /var/lib/mysql
    environment:
      MARIADB_RANDOM_ROOT_PASSWORD: 1
      MARIADB_DATABASE: ${CFG_DB_NAME}
      MARIADB_USER: ${CFG_DB_USER}
      MARIADB_PASSWORD: ${CFG_DB_PASSWORD}

volumes:
  db: null

networks:
  default:
  proxy:
    name: ovh-proxy_default
    external: true
  router:
    name: minecraft-shared_default
    external: true